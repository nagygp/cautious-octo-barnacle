import ConjecturesMTupleTripleCount.MTuple.Count
import ConjecturesMTupleTripleCount.MTuple.Disproof

/-!
# `MTuple` — aggregator module

This module re-exports the whole `MTuple` directory so that a single
`import ConjecturesMTupleTripleCount.MTuple` brings the m-tuple / triple-count
development (`Count` and the `Disproof`) into scope.
-/
